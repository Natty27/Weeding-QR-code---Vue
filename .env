# Port the Vite dev/preview server listens on
VITE_FRONTEND_PORT=47312

# Port the NestJS backend listens on
VITE_BACKEND_PORT=47311

# Fallback host used when window.location.hostname is unavailable
VITE_FALLBACK_HOST=192.168.0.108
